# Immich Configuration
# Your Immich server URL (include protocol: http:// or https://)
IMMICH_INSTANCE_URL=https://your-immich-server.com

# Your Immich API key (generate this from your Immich user settings)
IMMICH_API_KEY=your_api_key_here

# Number of concurrent uploads (default: 4, adjust based on your network and server)
IMMICH_UPLOAD_CONCURRENCY=4

# Comma-separated glob patterns to exclude from upload
# Default: **/issues/**,**/needs matching/**
IMMICH_IGNORE_PATTERNS=**/issues/**,**/needs matching/**

# Temporary Directory
# Directory for preprocessing temporary files (default: ../pre)
TEMP_DIR=../pre

# Disable Temporary File Cleanup
# Set to 1, true, or yes to preserve temporary files after processing (useful for debugging)
# Default: cleanup enabled
DISABLE_TEMP_CLEANUP=false
